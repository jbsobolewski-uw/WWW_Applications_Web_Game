{
  "name": "hex-minesweeper",
  "version": "1.0.0",
  "type": "module",
  "description": "TypeScript compiler pipeline for Hex-Sweeper App",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc --build",
    "build:fresh": "tsc --build --clean && tsc --build",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}